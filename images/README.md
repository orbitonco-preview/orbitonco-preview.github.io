Content imagery goes here, in WebP, referenced with explicit `width`/`height`.

It is empty on purpose: Orbit has supplied no photography, and section 9.1.3 of
the brief rules out generic stock. Where an image is missing, the site renders a
text-only block rather than a stock photo. See CLIENT-CHECKLIST.md section 3.
